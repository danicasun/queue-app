# Queue

This repo contains:
- `/ui-reference`: Base44-generated UI reference (Vite + React). Not production code.
- `/design/screenshots`: Visual source of truth for the Queue UI.

Next steps:
- Rebuild UI in Cursor using these references.
- Add backend + auth in Cursor.